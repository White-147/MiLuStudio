name: image_generation
display_name: Image Generation
description: Converts image prompt requests into deterministic mock placeholder asset records.
stage: MOCK_IMAGES_READY_FOR_REVIEW
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: true
  policy: pause_for_image_review

