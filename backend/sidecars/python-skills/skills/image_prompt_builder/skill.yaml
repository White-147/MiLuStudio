name: image_prompt_builder
display_name: Image Prompt Builder
description: Builds image prompt requests from a reviewed storyboard, character bible, and style bible.
stage: IMAGE_PROMPTS_READY_FOR_MOCK_GENERATION
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: false
  policy: auto_continue_to_mock_image_generation

