name: voice_casting
display_name: Voice Casting
description: Builds reviewable voice profiles and TTS task requests from script and storyboard timing.
stage: VOICE_TASKS_READY_FOR_REVIEW
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: true
  policy: pause_for_voice_cast_review
