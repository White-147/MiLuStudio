name: subtitle_generator
display_name: Subtitle Generator
description: Builds SRT-ready subtitle cues from script, storyboard timing, and voice tasks.
stage: SUBTITLES_READY_FOR_EDIT_PLAN
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: false
  policy: auto_continue_to_edit_plan
