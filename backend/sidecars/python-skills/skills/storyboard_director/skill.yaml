name: storyboard_director
display_name: Storyboard Director
description: Builds a reviewable shot list from an episode script, character bible, and style bible.
stage: STORYBOARD_READY_FOR_REVIEW
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: true
  policy: pause_for_storyboard_review

