name: character_bible
display_name: Character Bible
description: Builds stable character settings from a reviewable episode script.
stage: CHARACTER_BIBLE_READY_FOR_REVIEW
input_schema: schema.input.json
output_schema: schema.output.json
timeout_seconds: 20
max_retries: 1
cost_policy:
  model_required: false
  estimated_cost: 0
required_provider_capabilities: []
checkpoint_policy:
  required: true
  policy: pause_for_character_review

